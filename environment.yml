name: led-matrix
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.8
  - pillow=12.0.0
  - colorama=0.4.6
  - configargparse=1.7.1
  - pip
  # bleak: conda-forge has no aarch64/armv7l builds (Pi); install via pip (or piwheels on Pi)
  - pip:
    - bleak==2.0.0
    - pypixelcolor==0.4.0
    - freeweather==0.1.1
